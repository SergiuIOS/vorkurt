export const routerArray = Object.freeze({
  router: [
    {
      name: 'Pull Request',
      path: '/pull_request'
    } as { name: string, path: string },
    {
      name: 'Code',
      path: '/code'
    } as { name: string, path: string }, {
      name: 'Settings',
      path: '/settings'
    } as { name: string, path: string }
  ]
})
