import { ElixDragDirective } from './elix-drag.directive';

describe('ElixDragDirective', () => {
  it('should create an instance', () => {
    const directive = new ElixDragDirective();
    expect(directive).toBeTruthy();
  });
});
